import {Component,inject,OnInit} from '@angular/core'; import {ActivatedRoute} from '@angular/router'; import {CommonModule} from '@angular/common'; import {FormsModule} from '@angular/forms'; import {ApiService} from '../core/api.service'; import {PoTableModule,PoButtonModule,PoFieldModule} from '@po-ui/ng-components'; @Component({standalone:true,imports:[CommonModule,FormsModule,PoTableModule,PoButtonModule,PoFieldModule],template:`<div class="page"><h1>{{title}}</h1><div class="actions"><po-button p-label="Atualizar" (p-click)="load()"></po-button></div><table><thead><tr><th *ngFor="let c of columns">{{c}}</th></tr></thead><tbody><tr *ngFor="let row of items"><td *ngFor="let c of columns">{{row[c]}}</td></tr></tbody></table><p>Página {{page}} de {{totalPages}} — {{total}} registros</p><div class="actions"><po-button p-label="Primeira" [p-disabled]="page<=1" (p-click)="go(1)"></po-button><po-button p-label="Anterior" [p-disabled]="page<=1" (p-click)="go(page-1)"></po-button><po-button p-label="Próxima" [p-disabled]="page>=totalPages" (p-click)="go(page+1)"></po-button><po-button p-label="Última" [p-disabled]="page>=totalPages" (p-click)="go(totalPages)"></po-button></div></div>`}) export class ListComponent implements OnInit{private api=inject(ApiService);private route=inject(ActivatedRoute);resource='';title='';items:any[]=[];columns:string[]=[];page=1;totalPages=1;total=0;ngOnInit(){this.resource=this.route.snapshot.data['resource'];this.title=this.route.snapshot.data['title'];this.load()}load(){this.api.get<any>(this.resource,{page:this.page,pageSize:10}).subscribe(r=>{this.items=r.items;this.total=r.total;this.totalPages=r.totalPages||1;this.columns=this.items.length?Object.keys(this.items[0]).filter(x=>!['tenantId','tenant','transacoes'].includes(x)):[]})}go(p:number){this.page=p;this.load()}}