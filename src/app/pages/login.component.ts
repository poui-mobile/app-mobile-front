import {Component,inject} from '@angular/core'; import {FormsModule} from '@angular/forms'; import {Router} from '@angular/router'; import {PoFieldModule,PoButtonModule} from '@po-ui/ng-components'; import {AuthService} from '../core/auth.service'; @Component({standalone:true,imports:[FormsModule,PoFieldModule,PoButtonModule],template:`<div class="page"><div class="card form"><h1>Controle Financeiro</h1><po-input name="email" p-label="E-mail" [(ngModel)]="email"></po-input><po-password name="senha" p-label="Senha" [(ngModel)]="senha"></po-password><po-button p-label="Entrar" (p-click)="login()"></po-button><p class="error">{{error}}</p></div></div>`}) export class LoginComponent{email='admin@example.com';senha='admin123';error='';private auth=inject(AuthService);private router=inject(Router);login(){this.auth.login(this.email,this.senha).subscribe({next:()=>this.router.navigateByUrl('/'),error:e=>this.error=e.error?.message||'Falha no login'});}}